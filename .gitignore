__pycache__/
*.pyc

# State never lives in this repo - it defaults to ../.amp (see AMP_HOME).
# These entries are belt-and-braces in case someone points AMP_HOME here.
.amp/
.secrets.json
.board.json
config.json
packets/
rulings/

# Server log from a locally launched console.
.amp-server.log
